{
  "name": "disparo",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Automação de comentários do Facebook para o Messenger",
  "scripts": {
    "build": "npm install --prefix web && npm run build --prefix web",
    "server": "node src/server.js",
    "poller": "node src/engine/executor.js",
    "broadcast-worker": "node src/broadcastWorker.js",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "express": "^4.19.2",
    "multer": "^2.2.0",
    "pg": "^8.11.5"
  }
}
